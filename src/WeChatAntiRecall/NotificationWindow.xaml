<Window x:Class="WeChatAntiRecall.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="340"
        Height="140"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        AllowsTransparency="True"
        Background="Transparent">
    <Border CornerRadius="14"
            Background="#FF1F2937"
            BorderBrush="#55374151"
            BorderThickness="1"
            Padding="18">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="10"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="TitleTextBlock"
                       FontSize="17"
                       FontWeight="SemiBold"
                       Foreground="White"
                       Text="WeChat AntiRecall"/>
            <TextBlock Grid.Row="2"
                       x:Name="ContentTextBlock"
                       TextWrapping="Wrap"
                       Foreground="#FFF3F4F6"
                       Text=""/>
        </Grid>
    </Border>
</Window>
